import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zeppelinlabs.zescanbarcode',
  appName: 'ZeScan Barcode',
  webDir: 'out'
};

export default config;
